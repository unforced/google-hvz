%h1 Listing tags

%table.table
  %tr
    %th Tagger
    %th Tagee
    %th Time
    %th

  - @tags.each do |tag|
    %tr
      - if tag.tagger
        %td= tag.admin_tag ? 'Admin' : tag.tagger.user.name
      - else
        %td Someone?
      %td= tag.tagee.user.name
      %td= tag.time
